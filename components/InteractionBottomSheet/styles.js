import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    header: {
        flexDirection: 'row',
        marginTop: 10,
        alignItems: 'center',
    },
    actionBar: {
        marginRight: 5,
    },
    title: {
        flex: 1,
    },
});

export default styles;